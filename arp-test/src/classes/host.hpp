#include <string>

class Host
{
    private:
        string ip;
        string mac;
        stirng interface;

    public:
        Host(string ip, string mac, string interface)
        PingRow ping() {}
}